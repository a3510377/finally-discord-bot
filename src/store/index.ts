import { createStore } from "vuex"
import botModules from "./modules/bot"

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    botModules,
  },
  getters: {},
})
